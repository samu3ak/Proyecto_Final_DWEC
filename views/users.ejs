<%- include("template/head.ejs", {tituloWeb:"Usuarios", usuario})%>
    </head>

    <body class="usuarios">
        <%- include("template/header.ejs", {tituloWeb:"Usuarios"})%>

            <main class="container-fluid mainusuarios">
                <div class="container-fluid p-5">
                    <h1 class="display-1 text-light">Usuarios</h1>
                    <h1 class="display-6 text-light">
                        <%=title%>
                    </h1>
                </div>
                <div class="otro">
                    <div class="row justify-content-around">
                        <div class="col-6 text-center bg-secondary rounded border border-secondary border-1">
                            <table class="table">
                                <thead class="thead-dark fs-5">
                                    <tr>
                                        <th scope="col">Nombre de usuario</th>
                                        <th scope="col">Nombre</th>
                                        <th scope="col">Apellidos</th>
                                        <th scope="col">Rol</th>
                                        <%if (usuario.rol=="Profesor" ){%>
                                            <th scope="col">Acción</th>
                                            <%}%>
                                    </tr>
                                </thead>

                                <tbody class="border-bottom border-dark text-light">

                                    <!--Haremos el foreach para recorrer el array-->
                                    <% arrayUsuarios.forEach(usuarios=> { %>
                                        <tr>
                                            <td>
                                                <%= usuarios.nombre %>
                                            </td>
                                            <td>
                                                <%= usuarios.nombreReal %>
                                            </td>
                                            <td>
                                                <%= usuarios.apellidos %>
                                            </td>
                                            <td>
                                                <%= usuarios.rol %>
                                            </td>
                                            <%if (usuario.rol=="Profesor" ){%>
                                                <td>
                                                    <a class="btn btn-warning btn-sm" href="/login">Editar/Eliminar</a>
                                                </td>
                                                <%}%>
                                        </tr>
                                        <% }) %>
                                </tbody>
                            </table>
                        </div>
                        <div class="col-7 text-center mb-5">
                            <%if (usuario.rol=="Profesor" ){%>
                                <a class="btn btn-secondary btn-block my-4 me-5" href="/register">Nuevo Usuario</a>
                                <%}%>
                                    <a class="btn btn-secondary btn-block my-4" href="/">Volver</a>
                        </div>
                    </div>
                </div>

            </main>

            <%- include("template/footer.ejs")%>